# isolate configuration for the docker worker image. The entrypoint creates
# a delegated cgroup v2 subtree at /sys/fs/cgroup/isolate.
box_root = /var/local/lib/isolate
lock_root = /run/isolate/locks
cg_root = /sys/fs/cgroup/isolate
first_uid = 60000
first_gid = 60000
num_boxes = 1000
