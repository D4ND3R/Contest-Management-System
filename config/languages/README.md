# Language definitions

One YAML file per programming language. Adding a language only requires a
new file here (no code changes). The format is documented in
`docs/en/languages.md` / `docs/es/languages.md`.
