# Adivina el número

Hay un número secreto entre 1 y 10^9. Escribe `? x` y lee `<` (el secreto es
menor), `>` (es mayor) o `=`. Cuando lo sepas, escribe `! x`. Tienes a lo más
30 preguntas. Vacía la salida (`fflush`) después de cada línea.
