# Codifica y decodifica

Implementa `std::string encode(long long n)` y `long long decode(const
std::string &m)`: la primera ejecución codifica `n`; la segunda, sin memoria
compartida, debe recuperarlo del mensaje.
